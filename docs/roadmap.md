# remember & change header colors
- abilitity to remember chosen colors & reassign them to same project on restart
- web ui, in our coce, started with button in stream deck. This ui will host config stuff. First thing is way to change the header colors of the projects

# JetBrains (PhpStorm) companion, matching what extension/ does for VS Code
- terminal reveal, window-state publishing, terminal names, restore/backup commands
- feasibility spiked and it checks out on public IntelliJ Platform API — see
  docs/roadmap-jetbrains-companion.md for what was confirmed and what's next